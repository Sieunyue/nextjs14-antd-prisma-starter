import { FC, PropsWithChildren } from 'react'
import LoginForm from '@/app/admin/login/login'

const LoginPage: FC<PropsWithChildren> = () => {
  return (
    <div>
      <LoginForm />
    </div>
  )
}

export default LoginPage
